<div class="cGrid">
	{
		Array.from({ length: 48 }, (_, i) => (
			<div class="cGrid__line" data-id={i + 1} />
		))
	}
</div>

<style lang="scss">
	@use '/src/assets/scss/global' as *;

	.cGrid {
		position: fixed;
		inset: 0;
		z-index: -1;
		display: grid;
		grid-template-rows: auto;
		grid-template-columns: repeat(var(--grid-columns), 1fr);
		pointer-events: none;

		&__line {
			border-left: 1px solid
				var(--grid-border-color, var(--color-black-2));
			opacity: var(--grid-border-opacity, 1);

			&:nth-child(n + 25) {
				display: none;
			}

			&:first-child {
				border-left: none;
			}
		}
	}

	@include pc {
		.cGrid {
			&__line {
				&:nth-child(n + 25) {
					display: block;
				}
			}
		}
	}
</style>
